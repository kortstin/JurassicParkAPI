<%= form_with(model: cage) do |form| %>
  <% if cage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(cage.errors.count, "error") %> prohibited this cage from being saved:</h2>

      <ul>
        <% cage.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :max_capacity %>
    <%= form.number_field :max_capacity %>
  </div>

  <div class="field">
    <%= form.label :power_status %>
    <%= form.text_field :power_status %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
